import { screen, render } from "@testing-library/react";
import WorkoutBuilder from "../../../../components/builder/workout";

describe("WorkoutBuilder", () => {
  test("workout name input responds to user input", () => {});
});
