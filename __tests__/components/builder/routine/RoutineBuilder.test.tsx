import { screen, render } from "@testing-library/react";
import RoutineBuilder from "../../../../components/builder/routine";

describe("RoutineBuilder", () => {
  test("routine name input responds to user input", () => {});
});
